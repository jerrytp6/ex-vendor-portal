# 前端建置時的環境變數（Vite 在 build 時注入）
#
# dev：未設 → 預設使用 vite proxy `/api` → 後端 :7002
# prod 同網域：VITE_API_BASE_URL=/api（前端與後端在同 domain）
# prod 跨網域：VITE_API_BASE_URL=https://ex-api.zeabur.app
VITE_API_BASE_URL=

# 前端 base path（部署在子路徑時用，例如 /EXPO/）
# Zeabur 根路徑部署 → 留空或 /
VITE_BASE=/EXPO/
